{
  "tasks": {
    "test": "deno run -A ./cli.ts --dry --allowUncommitted",
    "release": "deno fmt && deno lint && deno run -A ./cli.ts"
  },
  "fmt": {
    "files": {
      "exclude": ["CHANGELOG.md"]
    }
  }
}
